# Shared Module

Common utilities, models, and configurations used across all modules.

## Responsibilities
- Data models and schemas (documents, chunks, queries)
- Configuration management and environment variables
- Common utilities (logging, validation, serialization)
- Database connection and session management
- Exception handling and error definitions
- Constants and enums used throughout the application
- Base classes and interfaces for extensibility